# digital-master-sheet-pages

Public GitHub Pages output for the private `cafeore-tkb/digital-master-sheet` source repository.

Only built files under `site/` are published. The source dataset is bundled into a hashed JavaScript chunk; no raw JSON file is deployed.
